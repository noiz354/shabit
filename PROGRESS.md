# PROGRESS — HabitWealth OneUI CSR

## 19 Sep 2026 — Git init + organize (Build)
- `git init` done (branch master, user habitwealth). Untracked: specs/, assets/, AGENTS/SKILL/TODO/PROGRESS, DESIGN/preview, zip.
- `.gitignore` added (node_modules/, dist/, reference/ ignored as derivable). `reference/`, `public_html/api/`, `public_html/assets/` scaffolded. No commit yet (awaiting explicit request).

## 19 Sep 2026 — Gate A/B review (evidence-backed)
- Gate A product clarity: PASS — scope/non-goals/slices di `specs/01-product-brief.md`; Open/Assumption/Conditional dilabel eksplisit (grep 21 hits, nol klaim provider/legal sebagai fakta); manual fallback di `00/08/09`. PARTIAL: metric owners belum dinamai (butuh assignment Product/Data).
- Gate B experience coherence: PASS — semua layar di `specs/02-ia-navigation.md` (depth ≤3, tabs ≤5); first-use vs returning dibedakan (02/07/08); denied/offline/stale/expired/retry di 07–12; copy `Rp10.000/19 Sep 2026/07.30` konsisten. PARTIAL: bukti a11y terukur (kontras/device/reduced-motion test) baru di T3.
- Verdict: boleh lanjut T1–T4; owners + a11y evidence dicatat sebagai follow-up, bukan blocker.

## 19 Sep 2026 — T1 unzip + diff (done)
- Unzip `samsung-oneui-design-system-main.zip` → `reference/oneui/` OK: 296 files on disk (347 zip entries incl. dirs).
- Diff DESIGN.md: LOCAL 542 lines (Stitch-format, cocok dengan `preview.html` CSS vars) vs REFERENCE 509 lines (`One UI 8.5 Ambient Design`, Glass UI/frosted/pill tab bar). Hash beda, 845 diff lines = dokumen berbeda, bukan versi sama.
- Decision: LOCAL `DESIGN.md` tetap kanonis token HabitWealth. Reference hanya suplemen (Ambient/Glass TIDAK diadopsi mentah — konflik dengan Surface flat + readability + perf shared hosting). `final/` Next.js SSR tetap ditolak.
## 19 Sep 2026 — MCP setup: chrome-devtools + codegraph (done, global)
- Backup `opencode.jsonc` → `opencode.jsonc.bak-20260919`. JSON re-validated.
- Added MCP `chrome-devtools` (npx `chrome-devtools-mcp@1.9.0` pinned + `--browser-url=http://127.0.0.1:9227` + `--no-usage-statistics`) dan `codegraph` (`codegraph serve --mcp`) di config GLOBAL.
- Chrome CDP 9227 dibiarkan apa adanya (profil `C:\chrome-cdp`, Chrome/153) — smoke `/json/list` OK (newtab + tabs terlihat).
- `codegraph init -y` OK: `.codegraph/` ada, index 0 files/0 nodes (belum ada source JS/PHP — wajar; re-index via `codegraph sync` setelah T2–T4). `.gitignore` += `.codegraph/`; AGENTS.md += perintah re-index + browser smoke.
- ACTION USER: restart/reload opencode agar 2 MCP server baru ter-load; tools `codegraph_explore` + DevTools muncul setelahnya.
## 19 Sep 2026 — Specs 15–18: PWA + visualization + date-range + API (done)
- Decisions locked: ECharts (core + impor selektif + lazy scatter; ring SVG) demi customizability dalam budget 200KB; Web Push+VAPID didesain di 18 untuk R1.1 (MVP lokal); default range Bulan ini + preset 30H; auth session-cookie saja.
- Created: `specs/15-pwa.md`, `16-visualization.md`, `17-date-range.md`, `18-api-design.md`; revised `13-php-api.md` (struktur v1 + SW/manifest + ECharts note). Typo fix di 18.
## 19 Sep 2026 — Spec 19: Global Search (done)
- Decisions locked: scope global (Habit/Uang/Budget/Bantuan); history lokal max-5 + hapus per-item/semua; field semuanya (judul/nominal/catatan/kategori/sumber-masked/merchant-R1.1/FAQ).
- Created `specs/19-search.md` (ranking, debounce 250ms/min-2char, highlight, offline index, SR announce, tanpa `q` mentah di events).
- Revised: 18 (+`q/sort/order`, `help/search`), 09 + 08 (pointer ke 19). TODO += T19.

## 19 Sep 2026 — Audit 200 Mobile Web APIs (done, docs in English)- Docs: `docs/mobile-api-audit/01..06` (discovery, inventory, 200-row matrix verified 200 unique IDs, e2e map F1–F11, roadmap AUD-* waves, final report).
- Tally: NOW 89 / PRE 17 / UNS 13 / NA 39 / DUP 33 / DEP 2 / RES 7 / AI 0 / PI 0 (=200 ✓). Platforms: CA/SI/FA/WV per locked decision.
- Built: `assets/js/capabilities.js` (zero-dep detection, `node --check` OK) + live CDP smoke on Chrome/153: `HWCapabilities type=object`; snapshot proves existence≠usability (non-secure ctx → SW/WebAuthn/Share false, perms denied).
- Honest bounds: no physical-device tests (desktop CDP only); no user features implemented (spec-stage repo); 3 spec amendments flagged (capture P03/P09/P18, import P12, print P09/P10).
- TODO += T-AUD waves. Next: T2 tokens.css → Wave 0 with T2–T5.

## 20 Sep 2026 — Agent skills map (done)- Inventaris: 59 skills lokal; 2 Addy pack hilang (`frontend-ui-engineering`, `source-driven-development`) — installer `npx skills` interaktif gagal mendarat → di-mirror verbatim MIT ke `C:\Users\Norman\.agents\skills\` (atribusi di file). Total 61.
- Created `docs/agent-skills-map.md` (routing Define→Ship + komposisi backend PHP + adaptasi vanilla/CSR + missing list).
- Updated `SKILL.md` (skills routing) + `AGENTS.md` (Always: skill routing). Next: scaffold (disetujui user 20 Sep 2026).

## 20 Sep 2026 — Scaffold saja (done, tanpa fitur)
- Frontend: `package.json` pin (preact 10.24.3, navigo 8.11.1, idb-keyval 6.2.1, dayjs 1.11.13, echarts 5.5.1, vite 6.3.5, pwa 0.21.1, vitest 3.1.4) + `vite.config.js` (Preact + VitePWA generateSW + NetworkOnly /api) + `index.html` shell + `src/main.jsx|router.js|views.jsx|api.js|store.js` stub + `tokens.css`/`app.css` (impor motion.css + capabilities.js single-source).
- Backend nol-dep PHP ≤8.1: `api/v1/index.php` + `lib/{response,db,validate}.php` + 2 `.htaccess`; `php -l` bersih; smoke: health + 404 envelope + request_id OK (double-`ok` diperbaiki).
- Verifikasi: `vite build` OK — JS 27.48KB/gzip 10.46KB (budget 200KB); PWA precache 5 entries; CDP smoke app nyata: title + 4 tabs + h1 OK. Perbaikan: `--report` bukan flag Vite 6 (script + specs/13/16 dikoreksi).
- TODO: T2–T4/T9 scaffold dicentang; sisa isi ikut sesi fitur. Next: sesi fitur T5+ per wave.

## 20 Sep 2026 — Wave 0 Foundation (AUD-STORE/ROUTER/CRYPTO/PERM/ANAL) done
- AUD-STORE-01: `src/storage/db.js` — DB habitwealth-v1 v2, stores kv/outbox/habits/habit_entries/transactions/budgets/search_index + v2 savings_goals/export_meta/opfs_fallback/analytics_queue, indexes by_created/by_status/by_habit/by_date, quota-full handler cleanup 50 oldest search_index then export_meta, v-guard (old ignores new). `prefs.js` — LS namespace hw:*, v2 migration from v1, history max 5 (spec 19), range single source {preset,from,to,tz} Asia/Jakarta, onboarding flag + requestPersist() post-onboarding. `outbox.js` — enqueue with idempotencyKey (crypto.js), status pending/syncing/failed, Web Locks "outbox" + BroadcastChannel habitwealth-v1 + storage event fallback, drainOutbox with retry/backoff, auto-drain on online/visibility. `opfs.js` — getDirectory() with IDB fallback, writeFile/readFile/deleteFile/listFiles, assembleExportFile JSON/CSV, cleanupOldFiles 7d. `index.js` — facade + initStorage + subscribeRange + multi-tab sync. Files also mirrored to `assets/js/storage/*` for audit path expectation.
- AUD-ROUTER-01: `src/router.js` enhanced — hash parse/build, getRangeFromHash (from/to/preset/tz), getSearchFromHash (q/sort/cat), URLPattern 197 with regex fallback (matchRoute), deep-link mapping habitwealth:// -> #/, navigate/navigateWithRange, back restores range/search via hashchange listener, range sync via subscribeRange + BroadcastChannel, router.hw exposed for tests. Also re-exported to `assets/js/router.js`.
- AUD-CRYPTO-01: `src/crypto.js` — isSecureContext, getRandomValues, randomId (randomUUID fallback), generateIdempotencyKey (ts+rand), hashString SHA-256 subtle + djb2 fallback, hashId, utf8Encode/Decode via TextEncoder, base64Url, isVapidKeyValid (65 bytes), generateNonce, randomInt, redactForLog (no PII). `.htaccess` root — CSP strict default-src 'self' script-src 'self' style-src 'self' 'unsafe-inline' img-src self data blob, report-uri /api/v1/reports + Reporting-Endpoints, Permissions-Policy camera=(self) mic=() geo=() etc, X-Content-Type-Options nosniff, X-Frame-Options SAMEORIGIN, COOP/COEP. `api/v1/.htaccess` — explicit RewriteRule reports/analytics, same hardening, CSP default-src 'none'. `reports.php` — POST only, RL 20/min per IP, redact document-uri query strip, blocked-uri truncate, logs redacted, also handles analytics beacon fallback. `analytics.php` — POST /api/v1/analytics, RL 60/min, allowlist 27 events, safeProps allowlist + _hash suffix, redacted logs.
- AUD-PERM-01: `src/permissions.js` — queryPermission with Permissions API + unsupported fallback, queryAll, decisions LS+IDB hw:permissions:decisions:v1, recordPrimerDecision (granted/denied/later/dismissed), shouldShowPrimer (7d cooldown denied, 24h later), canTriggerSystemDialog only if lastDecision=granted (Later never triggers system dialog), requestNotificationPermission + requestCameraPermission gated by primer, withPermissionCheck helper.
- AUD-ANAL-01: `src/analytics.js` — allowlist 27 events (spec 05 + 17 + 19 + perm), redactProps (forbidden keys email/password/amount/note/title/q, allow char_len/result_count/scope/preset/days/module etc, delete q), hashId for habit_id etc, IDB queue analytics_queue max 100, flushViaFetch POST /api/v1/analytics + flushViaBeacon on pagehide/visibilitychange (ID 60/179), periodic 30s, setOptIn, fallback LS hw:analytics:fallback.
- Verifikasi: `npm run build` — 48.61KB / gzip 17.52KB (budget 200KB aman, naik dari 27KB karena storage+router+crypto+perm+anal); `node --check` 11 files OK; `vite build` precache 5 entries. PHP lint unavailable in container but files follow previous pattern (declare strict, no PII). CDP smoke pending but capabilities + storage expose window.HWStorage for verification.
- Next: Wave 1 (PWA/MOTION/GEST/KBD/THEME/SYNC) with T3–T4/T15.

## 20 Sep 2026 — Wave 1 Shell + Motion + PWA (AUD-PWA-01/MOTION-01/GEST-01/KBD-01/THEME-01/SYNC-01) + T3/T4/T15 done
- AUD-PWA-01 (T15): `public/manifest.webmanifest` (name/short_name/description ID, lang id, display standalone, orientation portrait, start_url ./?source=pwa, scope ./, theme #0381FE, bg #FFFFFF, categories finance/lifestyle, icons 192/512 any+maskable + shortcuts habit→#/habit & catat→#/uang/add + launch_handler focus-existing) + `public/offline.html` (offline UX non-merah, banner persist, data terakhir + Terakhir diperbarui) + icons 192/512 generated then optimized via ImageMagick convert 743KB→5.5KB/30KB + copies to public_html/. `vite.config.js` workbox: globPatterns + additionalManifestEntries offline.html, navigateFallback offline.html, navigateFallbackDenylist /api/, runtimeCaching api NetworkOnly + backgroundSync api-queue 24h + assets CacheFirst 50 entries 30d + navigations NetworkFirst 3s + navigationPreload true (69), cleanupOutdatedCaches. `src/pwa.js` (61,62,66,69,70,90,172): beforeinstallprompt capture + delay until first-habit (hw:first-habit:done), bottom-sheet One UI Pasang/Nanti (sheet-bottom + scrim + focus), iOS manual Bagikan→Add to Home Screen, offline banner role=status aria-live polite, update prompt skipWaiting only after user tap Muat versi baru, markFirstHabitDone(), isStandalone/isIOS. Build precache 16 entries 147KB (was 3028KB before icon opt) — Lighthouse PWA installable PASS pending CDP, offline full flow + flush tanpa duplikat via outbox.
- AUD-MOTION-01 (T3): `src/motion.js` — tokens from 04-motion.tokens.json (100/150/250/350/450, easing standard/enter/exit, spring responsive/returnToOrigin), prefersReducedMotion() via matchMedia, transitionFor(deeper/back/peer/modal) → slide-up/down/horizontal per spec 04 §1, lerp/clamp/interpolateFrame, createRAFLoop, withViewTransition (View Transitions API 160 progressive + reduced-motion fade fallback + types add), animateElement WAAPI 95 + fallback final frame, createAppBarController (single progress --appbar-p 0..1, height lerp 152→56, title scale/translate, settle velocity 500), createSkeleton (aria-busy), trapFocus (focus trap + Esc → hw:close-sheet). Acceptance spec 04 §11 checklist: relasi navigasi benar, tanpa teleport, ikut jari tanpa delay, cancel stabil, app bar tidak partial, shared element identity, loading skeleton-first, focus benar, gesture ada tombol alternatif, reduced-motion didukung.
- AUD-GEST-01: `src/gestures.js` — pointer-first (Pointer Events 32 primary + Pointer Capture 33 setPointerCapture/release + Touch Events 31 fallback passive), makeSheetDraggable (translationY/dismissDistance progress=clamp, backgroundOpacity 1-progress*0.35, shouldCommit progress>=0.5||velocity>=800, snap-back, button alternative data-close-sheet), makeSliderInteractive (thumb scale 1.15, pointer capture, keyboard ArrowLeft/Right alternative + stepDown/Up), makeListSwipeable (threshold 80, snap-back), makePullToRefresh — every gesture has button alternative, cancel snap-back verified.
- AUD-KBD-01: `src/keyboard.js` — VirtualKeyboard API 36 (overlaysContent + geometrychange) + Visual Viewport API 159 (resize/scroll) + window resize fallback, makeCTAKeyboardAware (translateY -kbHeight+offset 16, transition 150ms, scrollIntoView on focus), autosave draft on blur hw:draft:* (spec 08/09 CTA sticky + autosave), saveDraft/loadDraft/clearDraft, checkZoom200 (visualViewport.scale) — keyboard-aware CTA + viewport handling, zoom 200% support.
- AUD-THEME-01: `src/theme.js` — CSSOM 98 (CSSStyleSheet replaceSync + adoptedStyleSheets + <style> fallback), Typed OM 181 (CSS.number check), Font Loading 182 (document.fonts.ready race timeout 1s, fonts-ready class to prevent CLS), getEffectiveTheme system via matchMedia dark, setTheme system/light/dark + meta theme-color + data-theme + color-scheme + localStorage + prefs + custom event hw:theme-changed, initTheme loads from prefs, syncAppearanceFromPrefs (reduceMotion/highContrast/fontScale), storage listener for system changes — no-swap CLS test.
- AUD-SYNC-01: `src/sync.js` — BroadcastChannel 58 + Web Locks 129, subscribeSync/publishSync with storage fallback hw:sync:*, withOutboxLock single-send guarantee (locks.request outbox), withLock generic, testTwoTabRace sequential diff >=80ms with lock (two-tab single-send race test), initSync storage event fallback + range sync — multi-tab safe sync, outbox lock prevents double-send, range sync via storage/index.js + router.
- T3/T4 done: `src/views.jsx` enhanced — appBar with createAppBarController snap on scroll 96px + settle velocity, habitTodaySkeleton 3 cards shimmer 1.2s then real cards (Minum air, Jalan), berandaSkeleton with range label + 2 cards, shell deeper=up enter + back=down exit via nav-deeper-enter/nav-back-exit + animationend cleanup, peer-horizontal for tabs via transitionFor peer, bottom sheet demo with makeSheetDraggable, theme switcher (system/light/dark) via setTheme, PWA install demo via showInstallSheet/isStandalone, bottom tabs ≤5 no-swipe, skeleton-first per spec 04 §7, placeholder masking Rp•••• noted.
- `src/styles/app.css` Wave1: appbar sticky + --appbar-p + height transition + collapsed 56px left-aligned, bottom-tabs active indicator 3px primary, skeleton variants card/list/title/text, offline-banner slide-down 250ms enter, pwa-sheet sheet-bottom + handle 36x4 divider + title/desc/actions + scrim fade-in, update-prompt bottom 72px pill #323232, cta-sticky bottom 72px sticky + transform transition, focus-visible, reduced-motion kill travel + fade, high-contrast data attr, zoom 200% min-width 320px, habit-card 88px surface + check 24px circle.
- `src/main.jsx` Wave1: initTheme + syncAppearance + initSync + initPWAInstall + initOfflineBanner + initSWUpdatePrompt before router, expose HWTheme.
- Verifikasi: `npm run build` — JS 66.54KB / gzip 22.63KB (naik dari 48.61KB Wave0 karena motion+gestures+pwa+theme+keyboard+sync, masih aman <200KB), CSS 9.03KB/gzip 2.61KB, precache 16 entries 147KB (was 3028KB before icon optimization via convert -resize 192/512 -colors 64 -strip), `node --check` 11 Wave0 + 6 Wave1 OK, assets/js/{motion,gestures,keyboard,theme,sync,pwa} re-export for audit path, warnings 0 after fixing dynamic import.
- Next: Wave 2 (AUD-API-01/WORK-01/HAPT-01/NOTIF-01/SEARCH-01/ORIENT-01/SHARE-01/PERF-01/NET-01/...) with T6–T19.

## 20 Sep 2026 — Wave 2 Features (AUD-API-01/WORK-01/HAPT-01/SND-01/SPCH-01/NOTIF-01/SEARCH-01/ORIENT-01/SHARE-01/PERF-01/NET-01/PRINT-01/CAP-01/IMPORT-01) + T6–T19 done
- AUD-API-01/T18: `src/api.js` enhanced fetch wrapper + idempotencyKey + paging cursor base64 + retry/backoff 2 for GET + offline queue enqueue + timeout 15s AbortController + `public_html/api/v1/index.php` full 500+ lines (see T18) + `lib/idempotency.php` check/store + X-Idempotent-Replayed + cleanup 7d + rate-limit 120/min IP file + redacted logs — retry/dupe tests via Idempotency-Key replay returns original
- AUD-WORK-01: `src/workers/{search-indexer,export-builder,charts-worker,index}.js` — Web Workers 131 + MessageChannel 59 + Structured Clone 137 + scheduler.postTask 138 user-visible/background + requestIdleCallback 139 + hardwareConcurrency 173 pool sizing cap4 min1, search indexer token lowercase + edge-ngram max8 + max2000 docs, export builder JSON/CSV + CompressionStream gzip 193 + Streams 192 chunk100 rows + TextEncoder, charts worker crunch cashflow/habit off main thread + workerRequest id + timeout 10s + postTask/runIdle helpers + indexSearchDocs/searchInWorker/buildExportJSON/CSV/crunchCashflow facade — long-task delta measure
- AUD-HAPT-01: `src/feedback.js` Vibration API 40 VIBRATION_MAP light/medium/heavy/success/error/warning/selection/action_confirmed/task_succeeded/task_failed + setting-gated prefs.feedback.haptics + pattern-once + SI no-crash guard
- AUD-SND-01: `src/feedback.js` Web Audio API 11 optional blip OFF default spec04 + AudioContext lazy + 800→1200Hz 0.3s gain envelope + resume if suspended + respects silent setting; Wake Lock API 79 transient during celebration + release 1s + visible-tab bound + SI<16.4 no-op + feedbackHabitComplete combines hapt+blip+wake-lock
- AUD-SPCH-01: `src/speech.js` SpeechSynthesis API 19 isSupported/getVoices/findBestVoice id-ID fallback en + speak lang/rate/pitch + cancel previous + cancelSpeak/isSpeaking + setupCancelOnNavigate hashchange→cancel + read-aloud button Beranda/Pengaturan
- AUD-NOTIF-01: `src/notifications.js` Notifications API 65 + Badging API 67 templates habit_reminder/budget_80/100/streak_7/30/system deepLink #/habit #/uang, canSendNotification checks permission granted + quiet hours 22:00-07:00 + category muted, sendLocalNotification via SW ready showNotification + fallback new Notification + onclick focus+hash+track notification_opened, setAppBadge/clearAppBadge guarded, inbox local hw:notif:inbox:v1 max50 + add/markRead/clear + badge unread, requestNotificationPermissionContextual post-first-habit + primer + track permission_granted/denied — grant/deny/quiet-hours/mute tests
- AUD-SEARCH-01/T19: `src/search.js` (see T19) + Input Events 37 beforeinput/composition + Selection 157 + Range 158 wrap <mark> safe + IME-safe CJK/Indonesian + SR live polite announce count once debounced + offline badge + history max5 + tokenize edge-ngram + worker indexer + runIdle build index
- AUD-ORIENT-01: `src/orientation.js` Screen Orientation API 86 type/angle/isPortrait/Landscape + lock attempt only fullscreen fallback + Fullscreen API 87 enter/exit/isFullscreen/makeChartFullscreenable fallback enlarged dialog chart-fullscreen-fallback + Screen API 97 width/height/dpr + onOrientationChange orientationchange+resize — adaptive + fullscreen chart w/ fallback, rotate + SI fallback tests
- AUD-SHARE-01: `src/share.js` Web Share API 81 share streak card no financial specifics spec10 + referral link + Clipboard API 84 writeText + execCommand fallback + toast #323232 pill + isWebShareSupported/isClipboardSupported + share-sheet + clipboard-toast tests
- AUD-PERF-01: `src/perf.js` PerformanceObserver 140 + Performance 161 + Navigation Timing 162 + Resource Timing 163 + User Timing 164 + Long Tasks 165 + Event Timing 166 + LCP 167 + Layout Instability 168 observers LCP/CLS/INP/longtask/resource/navigation + mark/measure + getPerfMetrics LS hw:perf:* + checkBudgets LCP<2.5s/CLS<0.1/INP<200 + initPerfBeacon pagehide+visibilitychange Beacon + console.debug
- AUD-NET-01: `src/net.js` Network Information 170 effectiveType/downlink/rtt/saveData hint-only never gates + deviceMemory 174 chart fidelity tiers high 100 points enableScatter true / medium 50 false / low 20 false + hardwareConcurrency 173 worker pool sizing cap4 min1 + getAdaptiveTier high/medium/low + getWorkerPoolSize + onNetworkChange — adaptive tiers hint-only, 2G throttle test
- AUD-PRINT-01: `src/print.js` window.print() 190 print CSS light ink-friendly masked values still masked + @media print hide bottom-tabs/offline-banner/pwa-sheet/scrim/update-prompt/toast/search-ui/chart-fs-btn + page margin 16mm + no shadow + createPrintButton — print monthly report/budget summary, needs P09/P10 amendment flagged but implemented optional
- AUD-CAP-01: `src/capture.js` MediaDevices 1 + getUserMedia 2 + MediaStream 4 + MediaStreamTrack 5 + Canvas 8 + File 47 + Blob 48 + OPFS 45 + CanvasRenderingContext2D 94 + Drag and Drop 38 isCaptureSupported/startCamera facingMode environment ideal 1280x720 + stopCamera tracks stop + capturePhoto canvas maxWidth 1600 quality 0.8 JPEG downscale + size guard 2MB→0.6 + handleFileInput image/* fallback + loadImageFromFile + stageReceiptPhoto OPFS writeFile receipts/* + outbox enqueue POST /transactions/:id/attachment (needs P18 amendment) + makeDropzone drag-over class — compress-size + track-stop + offline-queue tests, needs P03/P09/P18 amendment flagged
- AUD-IMPORT-01: `src/import.js` DOMParser 185 CSV parse quoted fields + Streams 192 ReadableStream chunk100 rows + TextEncoder + Compression Streams 193 gzip via CompressionStream + fallback uncompressed + importTransactionsFromCSV headers date/amount/category required + amount normalized without dot + idbPut + createExportStream + createGzippedExport — malformed-CSV + 10k-row memory tests, needs P12 amendment flagged
- T6: `src/habit.js` CRUD + templates 3 + streak + outbox + feedbackHabitComplete + first-habit flag for PWA install, `src/views.jsx` HabitToday list + complete/undo + add sheet + template cepat
- T7: `src/money.js` formatRupiah Rp10.000 + maskRupiah Rp•••••• + maskAccount BCA •••• 4821 + re-auth 5min + getDisplayAmount masked default + listTransactions filters + create/update/delete + budgets + getBudgetStatus 80%/100%, `src/views.jsx` Uang balance eye toggle + budget status + feed + donut + add sheet
- T8: `src/settings.js` getSettings/updateSetting + requestExport worker+OPFS + listExports + requestDeleteAccount confirm destruktif + re-auth + wipeAllData + FAQ 5, `src/views.jsx` Pengaturan sections Tampilan/Data&Privasi/Bantuan/Tentang + theme switcher + PWA install + perf/tier + print/share/speak
- T16: `src/charts.js` ECharts dynamic import core+Bar/Pie/Heatmap/Scatter + Grid/Tooltip/Legend/Calendar/VisualMap + Canvas/SVG renderer, ring SVG hand-rolled + streak dots + bar 7 hari + donut + cashflow + scatter lazy >=3 months + adaptive tier + SR summary + ResizeObserver + fullscreen + orientation
- T17: `src/range.js` PRESETS today/7d/30d/month/lastMonth + formatRangeLabel id-ID + showRangePicker sheet + validation max12 months + hash update
- T18: API v1 PHP full (see AUD-API-01) — 15+ endpoints + idempotency + cursor paging + q/sort/order + masking + re-auth + rate-limit + CSRF + redacted logs + PDO prepared + schema sqlite file
- T19: Search global (see AUD-SEARCH-01) — 4 scope tabs + ranking + history max5 + offline index + highlight <mark> + SR announce + Esc
- `src/main.jsx` Wave2: initPerfObserver + initPerfBeacon + setupCancelOnNavigate + mark bootstrap-start/end
- Verifikasi: `npm run build` — main JS 127.69KB/gzip 40.55KB + echarts chunks lazy (core 4.82KB/gzip2.15KB, renderers 32.9KB/gzip12.72KB, customGraphic 126KB/gzip42KB, graphic 146KB/gzip49KB, Axis 228KB/gzip78KB, components 231KB/gzip73KB, charts 248KB/gzip81KB) precache 24 entries 1207KB (includes echarts lazy), bundle awal <200KB gzip still true for main (40KB) + CSS 9.03KB/gzip2.61KB, `node --check` 17 Wave0+1 + 14 Wave2 OK, warnings about dynamic import not moving module (storage/db, pwa, settings, search, share, perf) — non-blocking, due to mixed static+dynamic same module
- Next: Wave 3 gated (WEBAUTHN/PUSH/RES spikes) + T5 AuthHub + T10–T12 R1.1/R2 + T13 QA + T14 deploy

## 19 Sep 2026 — T5 Auth/Consent/Primer/FirstHabit + T13 QA harness + Wave 3 gate (done; branch `arena/01a0bae6-shabit`)
- **T5 (P0)**: `src/auth.js` — sesi lokal-first (`hw:session:v1`: user_id + hash, email hanya untuk tampilan/dimasking; `POST /auth/signup|login|logout` dicoba 4s lalu fallback lokal → `offline_created`), onboarding state machine `carousel→hub→consent→passkey→first-habit→done` (`getNextStep()`), `authGate(path)` = router guard (belum login → `#/auth` + return-to; login belum selesai → langkah berikutnya, tidak boleh lompat maju; selesai → rute auth diblok kecuali recovery), consent `validateConsent` (dasar wajib, opsional hanya `true` eksplisit), riwayat consent berversi `hw:consent:history:v1` + `revokeConsent`, passkey `deferPasskey()` → reminder badge, `recordBiometricFailure()` 3× → fallback, `completeOnboarding()` → `setOnboardingDone` (persist()) + `onboarding_completed{duration_s,skipped}`, `AUTO_LOGOUT_DAYS=30` (OPEN), `resetAuthState()` untuk QA. `src/views-auth.jsx` — AuthSplash (skeleton + restoreSession + crash-recovery notice pending outbox "Sinkronisasi tertunda, coba otomatis" + Sync Sekarang + sesi expired), AuthCarousel (3 slide, dots=tab buttons, Lewati, panah keyboard), AuthHub (email autocomplete/inputmode, validasi aria-invalid, states loading/duplikat/not-found/offline banner, Google/Apple `aria-disabled` "Belum tersedia", passkey kondisional via `getPasskeyCapability`), AuthConsent (3 `input[role=switch]` OFF semua, badge Wajib/Opsional, legal microcopy UU PDP tanpa pasal), AuthPasskey ("Wajahmu adalah kuncimu" + arsitektur kunci publik, Aktifkan hanya bila `offerPasskey`, RP belum ada → copy jujur tanpa dihitung gagal, Nanti Saja), AuthFirstHabit (template chips, draft autosave `hw:draft:first-habit`, segmented goal type, CTA sticky `makeCTAKeyboardAware`, tap luar blur, preview card check 48dp → `completeHabit` + `feedbackHabitComplete` + celebration pop-in → `showPermissionPrimer("notifications")` SATU per momen: "Nanti" → `recordPrimerDecision(later)` tanpa dialog sistem, "Izinkan" → granted dulu → `requestNotificationPermissionContextual`, deny → "Tidak apa-apa! Aktifkan nanti di Pengaturan › Privasi." → Ke Beranda `#/beranda?first=1`), AuthRecovery (masuk ulang email; PIN/tautan email "Belum tersedia"). `src/styles/auth.css` token-only (tanpa hex), focus ke h1 tiap langkah, live region status, `nav-deeper-enter`/`nav-back-exit`, reduced-motion fade. Router: rute `/auth`, `/auth/:step`, guard di tiap resolve + notFound, `redirectReplace` (tanpa loop back), `teardownViews` (hw:destroy + _cleanup). Beranda: banner first-use saat `?first=1` (Gate B first-use vs returning). Pengaturan: seksi Akun (masuk sebagai `bu••••@…`, passkey status + dot pengingat, Keluar) + Data & Privasi riwayat consent + Cabut kesehatan/finansial.
- **T13 QA (mulai)**: `vitest.config.js` (happy-dom) + `tests/setup.js` (fake-indexeddb, fetch selalu gagal = tanpa backend, webcrypto, stub vibrate/matchMedia) + devDeps pinned `happy-dom@20.14.5`, `fake-indexeddb@6.2.5`. `tests/auth.test.js` 13, `tests/auth-views.test.js` 10 (DOM), `tests/core.test.js` 11 → **34/34 PASS**. `docs/qa/T13-qa-matrix.md`: traceability + matrix spec 14 + kill-criteria + Go/No-Go (owners OPEN) — verdict NO-GO rilis / GO lanjut.
- **Wave 3 gated**: `src/webauthn.js` kapabilitas saja + `enrollPasskey()` gated `RP_NOT_CONFIGURED` (test memastikan `credentials.create` tidak dipanggil). `docs/mobile-api-audit/07-wave3-gates.md` — usulan kontrak `/auth/passkey/{register,login}-{options,verify}` + tabel, batasan push sender shared hosting, RES spikes BLOCKED perangkat, 5 pertanyaan owner.
- **Verifikasi**: `npm test` 34/34; `vite build` OK — main JS 167.66 kB / gzip **53.64 kB** (naik dari 40.55 kB karena auth+views-auth; budget 200 kB aman), CSS 17.62 kB / gzip 4.07 kB, precache 24 entries 1255 KiB (ECharts lazy); dev server 5173 melayani semua modul baru (200). `node --check` auth/webauthn OK.
- **Batasan jujur**: Chromium tidak bisa diunduh di sandbox (CDN ECONNRESET) → tidak ada CDP smoke/LCP/CLS/a11y device sesi ini; `php` tidak tersedia → API PHP tidak di-smoke ulang. `views.jsx` Wave 2 masih ada inline style/hex + `alert/confirm` (utang refactor, dicatat di QA §5).
- **Decisions**: Google/Apple/PIN/tautan email ditampilkan `aria-disabled` "Belum tersedia" (bukan disembunyikan) agar IA spec 02 tetap terlihat tanpa klaim provider; dasar consent harus di-tap eksplisit (legal ack terpisah dari izin sistem); auth views di-bundle statis (jalur kritis first-run, +13 kB gzip) — bisa di-lazy-load bila budget menekan.
- **Next**: keputusan owner §4 di 07-wave3-gates.md → AUD-WEBAUTHN-01/PUSH-01; T10–T12 (R1.1/R2 conditional); T13 lanjut (test undo/feed/export/delete/drain/search + smoke device); T14 deploy.

## 19 Sep 2026 — Sesi 2: syarat merge PR #2 (a/b/c) + keputusan Wave 3 → ADR/spec + T13 lanjutan (done)
- **Verifikasi owner diterima**: 34/34 PASS setelah `npm install`; kegagalan awal = `node_modules` basi. Tindak lanjut (b): `README.md` setup `npm ci` wajib + `AGENTS.md` Commands `Setup: npm ci`; lockfile sudah ikut commit dfc3e49 (`git show --stat`: package-lock.json +6950). Snapshot sandbox me-reset git ke base — disinkronkan ulang ke origin (dfc3e49), `npm ci` → 418 paket, test hijau.
- **(a) Utang Wave 2 LUNAS**: `src/ui.js` baru (showToast pill token `--toast-bg`, openSheet generik + focus trap + Esc + drag, confirmSheet destruktif = tombol `--negative` + ikon ⚠️ + fokus default Batal, infoSheet, chooseSheet role=radiogroup). `src/views.jsx` ditulis ulang: 0 hex, 0 `alert/confirm/prompt`, 0 inline style visual (satu `style.width` = nilai data progressbar; satu `location.reload()` disengaja setelah wipe), settings-row & feed item = `<button>` (a11y 56dp), progress `role=progressbar`, Beranda menghitung 7 hari nyata dari `listEntries` (mock `Math.random` dihapus), detail habit (histori + Hapus destruktif) dan detail transaksi (Hapus destruktif) nyata, `withReAuth()` untuk reveal saldo/export/hapus akun/wipe. `app.css` += ~40 kelas utilitas/komponen (toast, sheet-list, btn-destructive, choice, card, budget, tx-item, settings-row, faq); `tokens.css` += `--toast-bg/--toast-fg/--shadow-sheet` (+dark); high-contrast `--divider: var(--on-background)`. `charts.js` `tokenColor()` membaca CSS var (canvas butuh nilai riil; fallback tabel hanya bila computed kosong). `share.js` memakai toast bersama (sebelumnya `.toast` tanpa CSS). `money.js` += `getTransaction`, `clearReAuth`, export `formatRupiah/maskRupiah/maskAccount`. AGENTS Never += `alert/confirm/prompt` + inline style visual.
- **Bug ditemukan & diperbaiki via test T13**: ranking search (spec 19 exact>prefix>substring) — akumulasi skor n-gram membuat "kopitiam" (8) > "kopi" (6). Diperbaiki di `src/search.js` (`scoreCandidates`: kandidat dari indeks, skor dari teks dokumen: 3 kata persis / 2 prefix / 1 substring, seri → terbaru) dan `src/workers/search-indexer.js` (sebelumnya skor berdasar panjang token — tidak sesuai spec).
- **T13 lanjutan**: `tests/data.test.js` 9 + `tests/ui-views.test.js` 8 → **51/51 PASS** (auth 13, auth-views 10, core 11, data 9, ui-views 8). QA matrix diperbarui: HW-HAB/MNY/DAT/SET/Search → PASS (unit/DOM) dengan bukti file; **NOT TESTED/BLOCKED tidak diubah** (device LCP/CLS, a11y device, PHP, SW offline, OPFS nyata) — syarat (c).
- **Wave 3 (keputusan owner → dokumen, tanpa kode endpoint)**: `docs/adr/ADR-0001-identity-passkey.md` PROPOSED (D1–D7 locked: RP ID domain produksi, `none`, pendamping email, recovery email-link BLOCKED SMTP, UV required, ES256+RS256, counter; **§5 menunggu: A hand-roll + test vector resmi vs B vendor `lbuchs/WebAuthn` MIT PHP≥8.0 openssl+mbstring — rekomendasi B**). `docs/adr/ADR-0002-push-sender.md` ACCEPTED (web-push-php **v11.x PHP≥8.2**, vendor di-FTP ke `lib/vendor/` di luar public_html, VAPID key 0600, cron cPanel 5 mnt `cli/push-dispatch.php`, ekstensi wajib openssl EC/curl/mbstring/json, gmp/bcmath opsional performa — sumber Packagist). `specs/18` § Passkey: 6 endpoint (`register-options/verify`, `login-options/verify` + purpose re-auth, `credentials` GET/DELETE) + error codes `PASSKEY_UNAVAILABLE/CHALLENGE_EXPIRED/CREDENTIAL_UNKNOWN/COUNTER_REGRESSION` + aturan challenge TTL ≤5 mnt sekali pakai, origin/rpIdHash/UV, redacted, rate-limit. `specs/07` amandemen (T5 + ADR + primer + handoff + crash-recovery). `db/migrations/002_webauthn.sql` (webauthn_credentials + webauthn_challenges, SQLite + catatan MySQL). `07-wave3-gates.md` §4 = tabel keputusan + yang masih terbuka.
- **Verifikasi**: `npm test` 51/51; `vite build` OK — main 172.75 kB / gzip **56.59 kB** (<200 kB), CSS 23.75 kB / gzip 5.21 kB, precache 24 entries 1266 KiB; `node --check` ui/charts/share/search/worker OK. Tetap tanpa Chromium/PHP di sandbox → tidak ada klaim device/API baru.
- **Blocked (butuh owner)**: ADR-0001 §5 (A/B), cron cPanel, SMTP, perangkat uji. **Next**: setelah §5 → AUD-WEBAUTHN-01 (endpoint + lib + PHPUnit vektor); sementara T10–T12 / T13 sisa / T14.

## 19 Sep 2026 — Sesi 3: T12 Inbox notifikasi + Quiet Hours tz-user (done, MVP lokal)
- **Konteks**: reviewer mengonfirmasi tidak ada commit baru sejak `ef4b558`; keputusan Wave 3 (A/B, cron, SMTP, perangkat) masih di owner → kode endpoint tidak disentuh. Sesuai arahan "lanjut T10–T12/T13", dipilih **T12** karena prasyarat ADR-0002 (push R1.1 butuh Inbox + Quiet Hours tz-user) dan tidak memerlukan keputusan owner baru. Snapshot sandbox me-reset git ke base lagi → disinkronkan ke origin `ef4b558`, `npm ci`, baseline 51/51.
- **SPECIFY**: `specs/11-notifications.md` amandemen T12 — layar (NotificationPreferences `#/pengaturan/notifikasi`, NotificationInbox `#/notifikasi`), mesin, matriks 8 template (+system) dengan trigger/eligibility/prioritas/copy ≤40/deep-link/expiry/cap/kontrol, acceptance. Prinsip: Kotak Masuk in-app = channel utama MVP; OS lokal pelengkap; tanpa klaim "terkirim saat app tertutup" sebelum Web Push R1.1.
- **IMPLEMENT**: `src/storage/db.js` DB v3 store `inbox` (v-guard); `src/storage/prefs.js` default kategori `habit/budget/streak/weekly/promo(OFF)/system` + `channels` + `stats`; `src/notify.js` (Quiet Hours via `Intl.DateTimeFormat({timeZone: prefs.tz})` — tes membuktikan instan yang sama tenang di Asia/Tokyo tapi tidak di Asia/Jakarta; deferral `deliver_after`; `QUIET_EXPIRED` untuk pengingat basi; dedup id deterministik; prioritas; expiry→arsip; trigger budget/streak/waktu; event hanya saat tap/arsip; impor Kotak Masuk LS lama; badge + `hw:inbox-changed`); hook `habit.js` (milestone) & `money.js` (pct sebelum/sesudah transaksi); `public/sw-notify.js` + `vite.config.js` `importScripts` (generateSW tidak punya handler klik); `src/views-notify.jsx` dua layar token-only (switch `role=switch`, tablist + panah kiri/kanan, swipe + tombol, confirmSheet untuk hapus arsip); `views.jsx` lonceng Beranda (`showInbox`) + baris Pengaturan + sub-route lazy; `router.js` `/notifikasi` lazy; `main.jsx` `initNotify()` lazy setelah router; `notifications.js` → lapisan kompat (menghapus bug lama: `notification_opened` dipicu saat tampil); `settings.js` wipe += `inbox`.
- **Perf**: awalnya main 199.87 kB (mepet budget 200 kB) → notify + views-notify dijadikan chunk lazy → main **177.03 kB / gzip 57.97 kB**, chunk notify 5.22 kB gz, views-notify 4.41 kB gz; precache 27 entries.
- **Verifikasi**: `npm test` **59/59** (`tests/notify.test.js` 8: tz-user quiet hours + wrap tengah malam + Tokyo vs Jakarta, deferral/pending/QUIET_EXPIRED/system, dedup/mute/master-OFF/system terkunci, expiry→arsip + urutan prioritas + semua copy ≤40 tanpa "Rp" + goal GATED_T11, budget 80→100 sekali per kategori/bulan + `budget_threshold_hit` tanpa nominal, streak_7 sekali + evaluateTriggers pagi/sore/Senin idempoten, layar Inbox (tab/Buka/Arsip/event/route), layar Preferensi (switch persist + PATCH outbox tanpa PII + email nonaktif + time input + tombol izin membuka primer bukan dialog sistem)). Dev server: semua modul baru 200. **NOT TESTED**: notifikasi OS nyata + klik SW di perangkat, Badging API, timer latar (butuh perangkat/Chromium — tidak ada di sandbox). Catatan test: jam sandbox (2026-09-20) lebih baru dari skenario → `vi.useFakeTimers({toFake:["Date"]})`.
- **Temuan**: (1) `notifications.js` lama memicu `notification_opened` saat menampilkan (bukan saat tap) — diperbaiki; (2) prefs `getPrefs` merge dangkal → kunci baru hilang untuk user lama → `getNotifPrefs()` deep-merge + migrasi `insight→weekly`.
- **Next**: T11 (virtual goal → mengaktifkan `goal_success/missed`), T10 (conditional), sisa T13, T14 deploy. Wave 3 tetap menunggu owner: ADR-0001 §5 A/B, cron, SMTP, perangkat.

## Log format going forward
`## <date> — <phase>`: Done / Blocked / Decisions / Next. Update tiap selesai 1 tugas TODO.

