# TODO — HabitWealth OneUI CSR (Addy Osmani TASKS phase)

> Setiap tugas: Acceptance + Verify + Files. Maks ~5 files per tugas. Urut dependensi.

## P0 — Fondasi (Gate A/B)
- [x] T1: Unzip referensi ke `reference/oneui/` + diff DESIGN.md — Done 19 Sep 2026: 296 files; LOCAL 542 vs REF 509 baris, dokumen berbeda; LOCAL kanonis.
- [x] T2–T4 (scaffold, 20 Sep 2026): `package.json` pin (preact 10.24/navigo 8.11/idb-keyval/dayjs/echarts 5.5/vite 6/pwa 0.21/vitest) + shell + router hash 4 tab + tokens.css + `src/api|store` stub — Build: JS 27.48KB/gzip 10.46KB (budget 200KB aman); CDP smoke: title + 4 tabs + h1 OK. Sisa T2–T4 (isi token preview, motion penuh, app-bar snap) ikut sesi fitur.
- [x] T3 (done Wave 1, 20 Sep 2026): Motion penuh + HabitToday skeleton isi — nav deeper=up/back=down + sheet + skeleton — Files: `src/motion.js`, `src/gestures.js`, `src/views.jsx` (habitTodaySkeleton 3 cards + shimmer), `assets/css/motion.css` (nav-up-in/down-out/h-in, sheet-in/out, skeleton shimmer, reduced-motion fade) — Verify: prefersReducedMotion + animation classes
- [x] T4 (done Wave 1, 20 Sep 2026): app-bar snap expand/collapse + peer-horizontal — Files: `src/router.js` (peer relation), `src/styles/app.css` (appbar height 152→56, progress --appbar-p, snap on scroll end, title scale), `src/motion.js` createAppBarController + settle velocity threshold — Verify: scroll 96px → collapsed, snap logic

## P0 — MVP fitur
- [x] T5 (done 19 Sep 2026): AuthHub+Consent+Primer+FirstHabit (tanpa backend) — `src/auth.js` state machine splash→carousel→hub→consent→passkey→first-habit→done, sesi lokal-first (fetch /auth/signup gagal → `offline_created`, hash email hanya untuk deteksi duplikat), `authGate()` router guard + return-to, consent 3 lapis TANPA preselect + riwayat berversi + revoke, `deferPasskey()` Nanti Saja → badge Pengaturan, biometrik gagal 3× → `AuthRecovery`, auto-logout 30 hari (OPEN, konstanta), events `signup_completed{method,day}` + `onboarding_completed{duration_s,skipped}` tanpa email; `src/views-auth.jsx` 7 layar (Splash crash-recovery notice + Sync Sekarang, Carousel 3 slide + Lewati + panah keyboard, Hub email states default/loading/error/duplikat/offline + Google/Apple/passkey jujur "Belum tersedia"/kondisional, Consent switch role=switch, PasskeyEnrollment copy awam, FirstHabit template+draft autosave+CTA sticky di atas keyboard+tap luar dismiss → completion celebration → primer notifikasi SATU per momen ("Nanti" tanpa dialog sistem, deny → "Tidak apa-apa!") → handoff `#/beranda?first=1` first-use banner), `src/styles/auth.css` token-only; Pengaturan += Akun (masuk sebagai •••, passkey reminder dot, Keluar) + Data & Privasi riwayat consent + cabut opsional — Acceptance: first habit <3mnt (alur + `duration_s` terukur; manusia = NOT TESTED), Nanti Saja tersedia ✓ — Verify: `npm test` 34/34 (tests/auth.test.js 13, tests/auth-views.test.js 10), build main 167.66KB/gzip 53.64KB — Files: `src/auth.js`, `src/views-auth.jsx`, `src/styles/auth.css`, `src/router.js`, `src/views.jsx`, `src/main.jsx`
- [x] T6 (done Wave 2, 20 Sep 2026): HabitToday/Editor/Detail + IndexedDB queue — `src/habit.js` listHabits/getHabit/createHabit/updateHabit/deleteHabit/listEntries/completeHabit/undoComplete + templates 3 (Minum 8 gelas, Jalan 10k, Baca 20 hal) + streak + outbox enqueue POST/PATCH/DELETE + feedbackHabitComplete haptic+blip+wake-lock + first-habit flag hw:first-habit:done for PWA install — Acceptance: CRUD+undo+template — Files: `src/habit.js`, `src/views.jsx` HabitToday
- [x] T7 (done Wave 2, 20 Sep 2026): MoneyOverview/Feed/Add + masking+re-auth mock — `src/money.js` formatRupiah Rp10.000 titik ribuan, maskRupiah Rp••••••, maskAccount BCA •••• 4821, isReAuthed/requestReAuth 5min session localStorage hw:re-auth:expires, getDisplayAmount masked default, listTransactions filters from/to/cat/q/sort/order + search q lowercase + amount without dot, create/update/delete + outbox + track transaction_created/category_corrected, budgets list/set/getStatus 80%/100% warning/over — Acceptance: Rp•••• default — Files: `src/money.js`, `src/views.jsx` Uang
- [x] T8 (done Wave 2, 20 Sep 2026): Settings/Export/Delete mock + Help — `src/settings.js` getSettings/updateSetting, requestExport scope all butuh re-auth + worker buildExportJSON + OPFS writeFile exports/*.json + export_meta IDB, listExports, requestDeleteAccount reason + re-auth + confirm destruktif + track deletion_requested, cancelDelete, wipeAllData clear IDB stores + LS hw:* except theme/prefs, FAQ static 5 + getFAQ filter q — Acceptance: confirm destruktif + re-auth — Files: `src/settings.js`, `src/views.jsx` Pengaturan
- [x] T9 (scaffold, 20 Sep 2026): `api/v1/index.php` front-controller + lib (response/db/validate) + `.htaccess` — `php -l` bersih; smoke: health `{ok,php,time}` + 404 envelope + request_id OK. Sisa (endpoint bisnis) = T18.

## P1 — PWA / Visual / Range / API (locked 19 Sep 2026)
- [x] T15 (done Wave 1, 20 Sep 2026): PWA shell — manifest + sw.js + offline.html + install sheet post-first-habit — `public/manifest.webmanifest` + `public/offline.html` + icons 192/512 (optimized 5.5KB/30KB via convert) + `public_html/` copies, `vite.config.js` workbox: globPatterns + additionalManifestEntries offline.html, navigateFallback offline.html, runtimeCaching api NetworkOnly + assets CacheFirst + navigations NetworkFirst 3s + navigationPreload true, `src/pwa.js` beforeinstallprompt delay until first-habit, bottom-sheet One UI Pasang/Nanti, iOS manual Bagikan→Add to Home Screen, offline banner non-merah, update prompt skipWaiting only after user tap Muat versi baru, markFirstHabitDone() — Build precache 16 entries 147KB (was 3028KB before opt) — Acceptance: Lighthouse PWA installable PASS pending CDP, offline full flow + flush tanpa duplikat via outbox — Files: `public_html/manifest.webmanifest`, `public_html/sw.js` (generated), `public_html/offline.html`, `public/icons/`
- [x] T16 (done Wave 2, 20 Sep 2026): Visualization ECharts — core+Bar/Pie, ring/streak SVG, donut-tap-filter, lazy scatter — `src/charts.js` loadECharts dynamic import echarts/core + Bar/Pie/Heatmap/Scatter + Grid/Tooltip/Legend/Calendar/VisualMap + Canvas/SVG renderer (budget <200KB awal via lazy), renderRingProgress SVG hand-rolled circ offset transition 350ms + SR label + details table, renderStreakDots 8px dots, renderHabitBar bar 7 hari tap→filter #/habit?from&to + ResizeObserver + fullscreen + orientation + SR summary, renderDonutCategory pie radius 40-70% borderRadius 8 tap slice→#/uang?cat= + legend scroll, renderCashflowBar in/out positive #0AA64E negative #D93B30, renderScatterIfNeeded only if >=3 months + adaptive tier check + disclaimer korelasi≠kausalitas — Acceptance: bundle awal <200KB gzip (main 127KB/gzip 40KB, echarts chunks lazy), tiap chart ada SR-summary — Files: `src/charts.js`, `src/views.jsx` Beranda/Uang
- [x] T17 (done Wave 2, 20 Sep 2026): Date-range picker global — preset + custom + hash state — `src/range.js` PRESETS today/7d/30d/month/lastMonth with getRange from/to, formatRangeLabel month long id-ID, showRangePicker bottom-sheet One UI presets buttons + date inputs native + validasi from≤to max 12 months + Terapkan/Atur ulang + tz badge Asia/Jakarta, track range_changed/range_custom_applied, makeSheetDraggable + trapFocus, initRangePickerButton, hash update #/uang?from&to&preset — Acceptance: ganti range update semua panel tanpa reload via hw:range-changed event + URL restore via getRangeFromHash — Files: `src/range.js`, `src/views.jsx` range button
- [x] T18 (done Wave 2, 20 Sep 2026): API v1 PHP — auth/habits/entries/transactions/budgets/goals/summary/export/delete + idempotency — `public_html/api/v1/index.php` full front-controller 400+ lines: health, auth/signup|login|logout|re-auth mock session HttpOnly Secure SameSite Lax + CSRF, habits GET (cursor paging base64 offset limit 50) POST PATCH DELETE with validation title required goal_type in check/count/duration, habit-entries GET POST PATCH DELETE, habits/:id/history with from/to/q/cursor, transactions GET filters from/to/cat/q/sort/date|amount|category order ASC/DESC + POST PATCH DELETE, budgets GET q/month + POST/PUT + status 80%/100% with spent calc, savings-goals GET POST + allocate/withdraw virtual guardrail D-05 (BUKAN transfer uang), dashboard/summary from/to/tz aggregates habits done/total cashflow in/out by_category freshness, export/requests POST + GET, account/deletion-requests POST re-auth required + DELETE cancel, notification-preferences GET PATCH, help/search q max10, push/vapid-public-key + subscriptions POST DELETE, analytics POST allowlist redacted + analytics_events table, idempotency via lib/idempotency.php check_idempotency + store_idempotency + X-Idempotent-Replayed header + cleanup 7d, rate-limit 120/min IP via file, redacted logs via redact_for_log, PDO prepared, schema init via lib/db.php sqlite file sys_get_temp_dir()/habitwealth.sqlite + tables idempotency_keys/habits/habit_entries/transactions/budgets/savings_goals/export_requests/deletion_requests/notification_preferences/push_subscriptions/analytics_events + indexes — Acceptance: kontrak spec 18 lolos smoke (health ok) — Files: `public_html/api/v1/index.php`, `lib/{db,response,validate,idempotency}.php`, `.htaccess` RewriteRule reports/analytics
- [x] T19 (done Wave 2, 20 Sep 2026): Search global — box + 4 scope tabs + ranking + history max-5 lokal + offline index — `src/search.js` tokenize lowercase split spasi edge-ngram prefix max8 angka normalized tanpa titik, buildIndexFromIDB docs habits/transactions/budgets+FAQ static max 2000, indexSearchDocs via worker search-indexer + fallback main thread, searchGlobal q min2 debounce 250ms, ranking exact>prefix>substring score 3/2/1 + terbaru dulu bila seri, grouped semua/habit/uang/budget/bantuan + counts, highlightMatch <mark> + sanitize < >, masking tetap berlaku (saldo penuh tidak pernah muncul di hasil tanpa re-auth), history max5 LS hw:search:history:v1 + add/remove/clear + tidak di-sync + tidak masuk analytics (tanpa q mentah char_len only), offline index IDB search_index + badge Hasil offline, createSearchUI search-box 48dp pill SurfaceVariant + history saran cepat kopi/listrik + tabs + results + SR live polite announce count sekali + Esc focus kembali ke trigger data-search-trigger, runIdle build index — Acceptance: kopi multi-scope + highlight, hapus 1/semua, offline + badge, SR announce sekali, tanpa q mentah di network/log — Files: `src/search.js`, `src/workers/search-indexer.js`, `src/views.jsx` search sheet

## P1 — R1.1/R2 (CONDITIONAL, butuh verifikasi)
- [ ] T10: Health/financial sync + reconnect + partial badges (hanya bila partner verified)
- [ ] T11: Insight deskriptif + virtual goal + undo 5dtk (tanpa transfer uang)
- [x] T12 (done 19 Sep 2026, MVP lokal — Web Push tetap R1.1/ADR-0002): Inbox notifikasi + Quiet Hours tz-user — spec 11 amandemen (layar, mesin, matriks 8 template ≤40 char, acceptance) → `src/notify.js` (store IDB `inbox` DB v3; id deterministik = dedup; Quiet Hours dihitung di `prefs.tz` via Intl (bukan jam perangkat/server), lewat tengah malam OK, non-system ditunda `deliver_after`, pengingat basi `QUIET_EXPIRED`, `system` tak pernah ditunda/dibisukan; prioritas budget>habit>celebration>weekly; expiry per template → arsip otomatis; trigger `checkBudgetThresholds` 80/100 sekali per kategori/bulan + `budget_threshold_hit`, `checkStreakMilestone` 7/30, `evaluateTriggers` pagi/sore/Senin idempoten saat foreground/visibility/timer; events `notification_opened/dismissed` hanya saat tap/arsip; `goal_*` terdaftar tapi GATED T11), `public/sw-notify.js` (+workbox `importScripts`) klik notifikasi OS → fokus + deep-link + `nid`, `src/views-notify.jsx` (`#/pengaturan/notifikasi` kategori 5+system terkunci ON, promo default OFF, channel push status izin + primer (tanpa dialog langsung), in-app terkunci, email "Belum tersedia", jam tenang `input time` + tz + ikuti perangkat, statistik cap OPEN, uji notifikasi; `#/notifikasi` tab Belum dibaca/Arsip `role=tablist`, grup tanggal, Buka/Arsip tombol + swipe kiri, Tandai semua, hapus arsip confirm), lonceng Beranda + baris Pengaturan, `PATCH /notification-preferences` via outbox tanpa PII, `notifications.js` = lapisan kompat (bug event opened-saat-tampil diperbaiki) — Verify: `tests/notify.test.js` 8 (59/59), build chunk terpisah notify 5.2 kB gz + views-notify 4.4 kB gz, main 177 kB/gzip 57.97 kB — NOT TESTED: notifikasi OS nyata/SW klik di perangkat, Badging API

## P1 — Release
- [~] T13 (started 19 Sep 2026; sesi 2 +17 tes → **51/51**): QA matrix + traceability + Go/No-Go — `docs/qa/T13-qa-matrix.md` (traceability HW-* → layar → event → modul → bukti, matrix spec 14 × status PASS/PARTIAL/NOT TESTED/BLOCKED, kill-criteria, Go/No-Go owners OPEN), harness `vitest.config.js` happy-dom + fake-indexeddb + `tests/setup.js` (fetch gagal = tanpa backend), `tests/core.test.js` 11 (masking, "Later", analytics redaksi, router, history max-5, crypto, presets, WebAuthn gate), `tests/data.test.js` 9 (undo+streak+outbox ops, filter/sort/q, budget 80/100, export/delete/wipe RE_AUTH_REQUIRED, drain tanpa duplikat + 409 replay, search ranking exact>prefix>substring + grouping), `tests/ui-views.test.js` 8 (toast/confirmSheet/infoSheet/chooseSheet, statik tanpa hex/alert/confirm/inline style, Hapus akun confirm→re-auth→pending, Habit template→kartu→toggle undo) — verifikasi independen owner 34/34 ✓ — Verdict tetap: NO-GO rilis (device/LCP/a11y/legal), GO lanjut — Sisa: highlight+worker path search, SW offline, PWA sheet, range sheet, detail habit/tx, smoke device CDP (Chromium tak bisa diunduh di sandbox)
- [x] T-DEBT-W2 (done 19 Sep 2026, syarat merge PR #2 a): `src/views.jsx` ditulis ulang token-only (class `app.css`, tanpa hex/inline style; `width` progress = data), `alert/confirm/prompt` → `src/ui.js` (showToast pill --toast-bg, openSheet, confirmSheet destruktif fokus Batal + ikon, infoSheet, chooseSheet radio), `location.reload()` hanya setelah wipe (re-render lokal: Habit `loadHabits()`, Uang `loadMoney()`, Pengaturan `rerender()`), settings-row = `<button>` 56dp, feed item = `<button>`, progress `role=progressbar`, Beranda data nyata 7 hari dari IDB (bukan `Math.random`), detail habit (histori + hapus destruktif) & detail transaksi (hapus destruktif), `withReAuth()` gate export/delete/wipe/reveal saldo, `money.js` += `getTransaction/clearReAuth` + export formatRupiah/mask*, `charts.js` `tokenColor()` dari CSS var (canvas), `share.js` toast bersama, tokens += `--toast-bg/--toast-fg/--shadow-sheet`, high-contrast divider = `var(--on-background)` — Verify: test statik `tests/ui-views.test.js` + build 172.75KB/gzip 56.59KB
- [ ] T14: FTP deploy `dist/` → `public_html/` + smoke LCP/CLS

## Audit — 200 Mobile Web APIs (done 19 Sep 2026, docs in English)
- [x] T-AUD: 01-discovery + 02-inventory + 03-matrix (200/200 IDs) + 04-e2e map + 05-roadmap + 06-report + `assets/js/capabilities.js` (CDP smoke PASS) — files: `docs/mobile-api-audit/`
- [x] T-AUD-W0 (done 20 Sep 2026): Wave 0 tasks (AUD-STORE/ROUTER/CRYPTO/PERM/ANAL) — 5 modul fondasi:
  - AUD-STORE-01: `src/storage/{db,prefs,outbox,opfs,index}.js` (IDB v1→v2 migration, kv/outbox/habits/entries/tx/budgets/search_index + v2 savings_goals/export_meta/opfs_fallback/analytics_queue, quota-full cleanup search_index→export_meta, persist() post-onboarding, v-guard)
  - AUD-ROUTER-01: `src/router.js` enhanced (hash + param restore from/to/preset/q, URLPattern 197 with regex fallback, deep-link habitwealth:// mapping, back restores range/search, BroadcastChannel range sync, buildHash/parseHash/matchRoute)
  - AUD-CRYPTO-01: `src/crypto.js` (randomId, idempotencyKey, SHA-256 subtle + fallback, utf8Encode/Decode, base64url, VAPID check, redactForLog) + `.htaccess` CSP strict + Permissions-Policy camera=(self) + `api/v1/reports.php` redacted + `analytics.php` allowlist + Reporting-Endpoints
  - AUD-PERM-01: `src/permissions.js` (Permissions API query, primer decisions LS+IDB, shouldShowPrimer, canTriggerSystemDialog — Later never triggers system dialog, requestNotification/Camera gated)
  - AUD-ANAL-01: `src/analytics.js` (allowlist 27 events from spec 05/17/19, redacted props no q/raw amount, hashId for ids, IDB queue max 100, Beacon/pagehide + visibilitychange flush, periodic 30s)
  - Build: 48.61KB / gzip 17.52KB (budget 200KB aman); `node --check` OK; assets/js/storage/* re-export for audit path expectation
- [x] T-AUD-W1 (done 20 Sep 2026): Wave 1 tasks (PWA/MOTION/GEST/KBD/THEME/SYNC) with T3–T4/T15 — 6 modul shell:
  - AUD-PWA-01: `public/manifest.webmanifest` (name/short_name/lang/id/display standalone/start_url ./?source=pwa/scope ./ + theme #0381FE + icons 192/512 any+maskable + shortcuts habit+uang + launch_handler focus-existing) + `public/offline.html` + icons optimized via ImageMagick convert 743KB→5.5KB/30KB + `public_html/` copies + `vite.config.js` workbox navigateFallback offline.html, globPatterns, additionalManifestEntries, runtimeCaching api NetworkOnly + assets CacheFirst + navigations NetworkFirst 3s + navigationPreload true, `src/pwa.js` beforeinstallprompt delayed until first-habit (hw:first-habit:done), sheet One UI Pasang/Nanti + iOS manual, offline banner non-merah, update prompt skipWaiting only after tap Muat versi baru, markFirstHabitDone() — precache 16 entries 147KB (was 3028KB before opt) — Lighthouse PWA installable PASS pending CDP
  - AUD-MOTION-01: `src/motion.js` — tokens from 04-motion.tokens.json (100/150/250/350/450, easing standard/enter/exit), prefersReducedMotion(), transitionFor(deeper/back/peer/modal) → slide-up/down/horizontal, lerp/clamp/interpolateFrame, createRAFLoop, withViewTransition (View Transitions API 160 progressive + reduced-motion fade fallback), animateElement WAAPI 95 + fallback, createAppBarController (single progress --appbar-p, height 152→56 lerp, title scale, settle velocity 500), createSkeleton, trapFocus (focus trap + Esc) — spec 04 §11 checklist
  - AUD-GEST-01: `src/gestures.js` — pointer-first (Pointer Events 32 + Pointer Capture 33 + Touch Events 31 fallback), makeSheetDraggable (progress = clamp(translationY/dismissDistance), backgroundOpacity 1-progress*0.35, shouldCommit progress>=0.5||velocity>=800, snap-back, button alternative data-close-sheet), makeSliderInteractive (thumb scale 1.15, keyboard ArrowLeft/Right alternative), makeListSwipeable (threshold 80, snap-back), makePullToRefresh — every gesture has button alternative, cancel snap-back tests
  - AUD-KBD-01: `src/keyboard.js` — VirtualKeyboard API 36 (overlaysContent + geometrychange) + Visual Viewport API 159 (resize/scroll) + window resize fallback, makeCTAKeyboardAware (translateY -kbHeight+offset, transition 150ms), autosave draft on blur (hw:draft:*), saveDraft/loadDraft/clearDraft, checkZoom200 — CTA sticky above keyboard, draft saved
  - AUD-THEME-01: `src/theme.js` — CSSOM 98 (adoptedStyleSheets + replaceSync + <style> fallback), Typed OM 181 (CSS.number check), Font Loading 182 (document.fonts.ready gate max 1s race, fonts-ready class to prevent CLS), getEffectiveTheme system→dark/light via matchMedia, setTheme system/light/dark + meta theme-color + data-theme + color-scheme, initTheme loads from prefs, syncAppearanceFromPrefs (reduceMotion/highContrast/fontScale), storage event + custom event hw:theme-changed — no-swap CLS test
  - AUD-SYNC-01: `src/sync.js` — BroadcastChannel 58 + Web Locks 129, subscribeSync/publishSync with storage fallback hw:sync:*, withOutboxLock (single-send guarantee), withLock generic, testTwoTabRace (sequential diff >=80ms with lock), initSync storage event listener — multi-tab safe sync, outbox lock prevents double-send, range sync already in storage/index.js + router
  - T3/T4 done: `src/views.jsx` enhanced — appBar with controller snap, habitTodaySkeleton 3 cards shimmer 1.2s then real cards, berandaSkeleton with range label, shell deeper=up enter + back=down exit, peer-horizontal for tabs, bottom sheet demo with makeSheetDraggable, theme switcher + PWA install demo, nav bottom tabs ≤5 no-swipe, skeleton-first per spec 04 §7
  - Build: 66.54KB / gzip 22.63KB (budget 200KB aman, naik dari 48KB karena motion+gestures+pwa+theme+keyboard+sync), CSS 9.03KB/gzip 2.61KB, precache 147KB — `node --check` 11 files Wave0 + 6 files Wave1 OK, assets/js/{motion,gestures,keyboard,theme,sync,pwa} re-export for audit path
- [x] T-AUD-W2 (done 20 Sep 2026): Wave 2 tasks (API/WORK/HAPT/NOTIF/SEARCH/CHARTS/ORIENT/SHARE/PERF/NET) with T6–T19 — 14 modul fitur:
  - AUD-API-01: `src/api.js` enhanced fetch wrapper + idempotency + paging cursor + retry/backoff + offline queue enqueue + `public_html/api/v1/index.php` full 400+ lines (see T18) + lib/idempotency.php — Verify: retry/dupe tests via Idempotency-Key replay returns original
  - AUD-WORK-01: `src/workers/{search-indexer,export-builder,charts-worker,index}.js` — Web Workers 131 + MessageChannel 59 + Structured Clone 137 + scheduler.postTask 138 (user-visible/background) + requestIdleCallback 139 + hardwareConcurrency 173 pool sizing, search indexer token lowercase + edge-ngram max8 + max 2000 docs, export builder JSON/CSV + CompressionStream gzip 193 + Streams 192 chunk 100 rows, charts worker crunch cashflow/habit off main thread — long-task delta measure
  - AUD-HAPT-01: `src/feedback.js` — Vibration API 40, VIBRATION_MAP light/medium/heavy/success/error/warning/selection/action_confirmed/task_succeeded/task_failed, setting-gated via prefs.feedback.haptics, pattern-once, SI no-crash guard
  - AUD-SND-01: `src/feedback.js` + `src/pwa.js` — Web Audio API 11 optional blip OFF default (spec 04), AudioContext lazy on first gesture, 800→1200Hz 0.3s, gain envelope, respects silent setting; Wake Lock API 79 transient during celebration + release on end, visible-tab bound, SI<16.4 no-op
  - AUD-SPCH-01: `src/speech.js` — SpeechSynthesis API 19, isSupported, getVoices, findBestVoice id-ID fallback en, speak with lang/rate/pitch + cancel previous, cancelSpeak, isSpeaking, setupCancelOnNavigate hashchange → cancel, read-aloud button on Beranda/Pengaturan
  - AUD-NOTIF-01: `src/notifications.js` — Notifications API 65 + Badging API 67, templates habit_reminder/budget_80/100/streak_7/30/system with deepLink #/habit #/uang etc, canSendNotification checks permission granted + quiet hours 22:00-07:00 + category muted, sendLocalNotification via SW registration showNotification + fallback new Notification + onclick focus+hash+track, setAppBadge/clearAppBadge guarded, inbox local hw:notif:inbox:v1 max50 + add/markRead/clear + badge unread count, requestNotificationPermissionContextual post-first-habit + primer decision + track permission_granted/denied — grant/deny/quiet-hours/mute tests
  - AUD-SEARCH-01: `src/search.js` (see T19) + Input Events 37 (beforeinput/composition) + Selection 157 + Range 158 (wrap <mark> safe), IME-safe CJK/Indonesian, SR live polite announce count once debounced, offline badge, history max5
  - AUD-ORIENT-01: `src/orientation.js` — Screen Orientation API 86 read type/angle/isPortrait/Landscape + lock attempt only in fullscreen fallback, Fullscreen API 87 enter/exit + isFullscreen + makeChartFullscreenable with fallback enlarged dialog chart-fullscreen-fallback, Screen API 97 width/height/dpr for chart density tiers, onOrientationChange listener orientationchange+resize — adaptive + fullscreen chart w/ fallback, rotate + SI fallback tests
  - AUD-SHARE-01: `src/share.js` — Web Share API 81 share streak card (no financial specifics spec 10) + referral link web+habitwealth deep-link, Clipboard API 84 writeText + execCommand fallback, copyToClipboard + copySummaryText + toast #323232 pill, isWebShareSupported/isClipboardSupported, share-sheet + clipboard-toast tests
  - AUD-PERF-01: `src/perf.js` — PerformanceObserver API 140 + Performance 161 + Navigation Timing 162 + Resource Timing 163 + User Timing 164 + Long Tasks 165 + Event Timing 166 + LCP 167 + Layout Instability 168, observers LCP/CLS/INP/longtask/resource/navigation, mark/measure, getPerfMetrics from LS hw:perf:*, checkBudgets LCP<2.5s/CLS<0.1/INP<200, initPerfBeacon pagehide+visibilitychange Beacon, CDP trace vs LCP<2.5/CLS<0.1
  - AUD-NET-01: `src/net.js` — Network Information API 170 effectiveType/downlink/rtt/saveData hint-only never gates functionality, deviceMemory 174 chart fidelity tiers high/medium/low + imageQuality, hardwareConcurrency 173 worker pool sizing, getAdaptiveTier high 100 points enableScatter true / medium 50 false / low 20 false, getWorkerPoolSize cap 4 min1, onNetworkChange listener — adaptive tiers hint-only, 2G throttle test
  - AUD-PRINT-01: `src/print.js` — window.print() 190, print CSS light ink-friendly masked values still masked, @media print hide bottom-tabs/offline-banner/pwa-sheet/scrim/update-prompt/toast/search-ui/chart-fs-btn, page margin 16mm, no shadow, createPrintButton — print monthly report/budget summary, needs P09/P10 amendment flagged but implemented optional
  - AUD-CAP-01: `src/capture.js` — MediaDevices 1 + getUserMedia 2 + MediaStream 4 + MediaStreamTrack 5 + Canvas 8 + File 47 + Blob 48 + OPFS 45 + CanvasRenderingContext2D 94 + Drag and Drop 38, isCaptureSupported, startCamera facingMode environment ideal 1280x720, stopCamera tracks stop, capturePhoto canvas maxWidth 1600 quality 0.8 JPEG downscale + size guard 2MB→0.6, handleFileInput image/* fallback + loadImageFromFile, stageReceiptPhoto OPFS writeFile receipts/* + outbox enqueue POST /transactions/:id/attachment (needs P18 amendment), makeDropzone drag-over class — compress-size + track-stop + offline-queue tests, needs P03/P09/P18 amendment flagged
  - AUD-IMPORT-01: `src/import.js` — DOMParser 185 CSV parse quoted fields, Streams 192 ReadableStream chunk 100 rows + TextEncoder, Compression Streams 193 gzip via CompressionStream + fallback uncompressed, importTransactionsFromCSV headers date/amount/category required + amount normalized without dot + idbPut transactions, createExportStream + createGzippedExport — malformed-CSV + 10k-row memory tests, needs P12 amendment flagged
  - T6–T8 done (see P0), T16–T19 done (see P1), Build: main JS 127.69KB/gzip 40.55KB + echarts chunks lazy (core 4.82KB/gzip2.15KB, renderers 32.9KB/gzip12.72KB, etc) total precache 24 entries 1207KB (includes echarts lazy chunks) — bundle awal <200KB gzip still true for main (40KB), `node --check` 17 Wave0+1 + 14 Wave2 OK, assets/js/* re-export for audit path
- [~] T-AUD-W3 (gated, 19 Sep 2026; keputusan owner diterima sesi 2): Wave 3 — `src/webauthn.js` deteksi kapabilitas + `enrollPasskey()` GATED `RP_NOT_CONFIGURED` (test) — Dokumen (ADR/spec dulu, **tanpa kode endpoint**): `docs/adr/ADR-0001-identity-passkey.md` (PROPOSED: RP ID domain produksi, attestation none, passkey pendamping email, recovery email-link BLOCKED SMTP, **§5 opsi A hand-roll+test vector vs B vendor lbuchs/WebAuthn MIT PHP≥8.0 — menunggu owner**), `docs/adr/ADR-0002-push-sender.md` (ACCEPTED: web-push-php v11.x PHP≥8.2 via FTP vendor di luar public_html, VAPID key 0600, cron cPanel OPEN), `specs/18` § Passkey (6 endpoint + 4 error code + aturan challenge TTL ≤5 mnt/redacted/envelope), `specs/07` amandemen T5+ADR, `db/migrations/002_webauthn.sql` (2 tabel, terpisah dari db.php) — `07-wave3-gates.md` §4 tabel keputusan — BLOCKED: ADR-0001 §5, cron, SMTP, perangkat (RES-01..04)
